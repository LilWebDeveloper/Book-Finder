import styled from "@emotion/styled";

export const LoadingWrapper = styled('div')(() => ({
    width: "100%",
    height: "100%",
    display: 'flex',
    alignItems: 'center',
    justifyContent: 'center'
  }))